# خارطة طريق حراج ستيشن

> آخر تحديث: 2026-08-30. هذا الملف يسجل الحالة الفعلية فقط؛ التفاصيل التشغيلية في `DEPLOY.md`.

## جاهز ويعمل

- Next.js 16 / React 19 مع PostgreSQL مُدار وPrisma migrations حقيقية.
- الإعلانات، المزادات، المزايدة بالوكالة، المتاجر، المحادثات، الإشعارات، الحملات والنقاط.
- Cloudflare R2 للصور العامة، ومخزن خاص لوثائق الهوية ومرفقات المحادثات.
- تسجيل البريد وكلمة المرور، تحقق البريد، reset مشفر، Google OAuth بمعرّف `sub`، و2FA بالبريد.
- بوابة إدارة منفصلة بجلسة OTP مستقلة، وRedis مشترك لعمال PM2.
- Cron كل دقيقة، فحص صحة، CSP/security headers، وحذف الحساب وفق دورة حياة الملفات.
- CI يشغّل lint وTypeScript وPostgreSQL migrations والاختبارات وbuild وproduction audit.
- نسخ يومي مشفر لقاعدة البيانات والملفات الخاصة مع دعم وجهة rclone خارجية.

## قرار الدفع الحالي

الدفع مؤجل إداريًا إلى حين اعتماد حساب بوابة سعودية. لذلك `PAYMENTS_ENABLED=false` في الإنتاج، وكل مسارات الشحن تفشل بصورة مغلقة ولا تمنح نقاطًا مجانًا. الكود يدعم Moyasar مع VAT وwebhook وidempotent credit، لكن لا يُفعّل قبل:

- اعتماد الحساب ومفاتيح الإنتاج.
- إنشاء `MOYASAR_WEBHOOK_SECRET` واختبار callback + webhook حقيقيًا.
- اعتماد سياسة استرداد وفاتورة ضريبية قابلة للطباعة.

## نواقص تحتاج خدمات أو قرارات خارج المشروع

| الأولوية | البند | شرط الإنجاز |
|---|---|---|
| عالية | مراقبة الأخطاء ووقت التشغيل | حساب Sentry/بديل + جهة uptime وتنبيهات |
| عالية | نسخة احتياطية خارجية متكررة | ضبط `BACKUP_REMOTE` في rclone واختبار restore ربع سنوي |
| عالية | تدوير الأسرار القديمة | تدوير R2/Moyasar/SMTP/VAPID من لوحات المزودين ثم تحديث `.env` |
| متوسطة | SMS OTP/نفاذ | حساب مزود سعودي وسياسة موثّق واضحة |
| متوسطة | staging مستقل | دومين وقاعدة وR2 وأسرار منفصلة عن الإنتاج |
| متوسطة | الدفع والفواتير والاسترداد | اعتماد البوابة والمتطلبات المحاسبية |
| منخفضة | إكمال الإنجليزية | مراجعة تحريرية لكل الرحلات |
| منخفضة | خرائط وميزات مقارنة | قرار منتج ومزود خرائط |

## بوابات أي إصدار

1. `npm run lint`
2. `npx tsc --noEmit`
3. `npm test`
4. `npm run build`
5. `npm audit --omit=dev --audit-level=high`
6. backup مشفر + preflight للبيانات + خطة rollback قبل الإنتاج
7. health check ومسارات عامة/دخول/إدارة بعد النشر

لا يُفعّل الدفع بتغيير إعداد لوحة الإدارة فقط؛ يلزم أيضًا `PAYMENTS_ENABLED=true` ومفتاح بوابة صالح وwebhook مختبر.
